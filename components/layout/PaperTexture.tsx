/* The paper the whole site is printed on. Two fixed SVG noise layers —
   a fibre wash and a grain speckle — so the sheet reads as one continuous
   surface while scrolling. Purely decorative. */
export function PaperTexture() {
  return <div className="paper-texture" aria-hidden />;
}
